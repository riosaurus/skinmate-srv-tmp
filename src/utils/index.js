const constants = require('./variables');
const middlewares = require('./middlewares');
const otpServer = require('./otp-server');
const errors = require('./errors');

module.exports = { constants, middlewares, otpServer, errors };
