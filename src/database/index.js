const Appointment = require('./Appointment');
const Client = require('./Client');
const Doctor = require('./Doctor');
const User = require('./User');
const Family = require('./Family_member');

module.exports = {
  Appointment, Client, Doctor, User, Family,
};
